{
  "name": "playwright-ts-framework",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright + TypeScript UI test framework. Tests live and run here. Capture YAML session packs with pagesnap-playwright, drop them in snapshots/, then generate specs and page objects.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^20.17.0",
    "typescript": "^5.6.3"
  }
}
